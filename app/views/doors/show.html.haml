
.js-door-partial
  = render @door

%br

.door-commands
  = link_to 'Open', open_door_path(@door), class: 'js-doors btn btn-success', remote: true
  = link_to 'Close', close_door_path(@door), class: 'js-doors btn btn-primary', remote: true
